<h3 class="pb-4 mb-4 fst-italic border-bottom">
  From the Firehose
</h3>
<nav class="blog-pagination" aria-label="Pagination">
  <span>
    <%= link_to 'New Blog', new_blog_path, class: "btn btn-outline-primary"  if logged_in?(:site_admin) %>
  </span>
  <span>
    <%= link_to 'Topics', topics_path, class: "btn btn-outline-secondary"  if logged_in?(:site_admin) %>
  </span>
</nav>
<%= paginate @blogs %>
<%= render @blogs %>
<%= paginate @blogs %>
